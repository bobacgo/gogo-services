# polaris
global:
  serverConnector:
    addresses:
      - polaris-headless:8091
config:
  configConnector:
    addresses:
      - polaris-headless:8093

# 自己服务本身需要的配置数据
_config:
  namespace: default
  group: gogo_v1.0.0
  filenames: ['admin-service.yaml', 'common.yaml', 'mysql.yaml', 'redis.yaml']